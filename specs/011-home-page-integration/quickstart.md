# Quickstart

No new dependencies or environment variables are required for this feature.

To test the home page integration locally:

1. Ensure the Supabase local container or remote project is accessible.
2. Run the development server: `npm run dev`
3. Visit `http://localhost:3000/` and verify the mixed layout featuring listings and community highlights.
