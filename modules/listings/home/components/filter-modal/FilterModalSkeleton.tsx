/**
 * Skeleton for FilterModal.
 * Since the modal is hidden by default, the skeleton is also hidden (null)
 * to prevent layout shifts or empty visible containers.
 * The modal content will become available once the data is loaded.
 */
export const FilterModalSkeleton = () => {
  return null;
};
